step 1:	install VS Code.
step 2:	install c# extension
step 3:	install .NET SDK
step 4: open cmd and type as follows
	
	1.dotnet new console -o "name"
	2.cd project name
	3.type code.
	4.run by using "dotnet run"